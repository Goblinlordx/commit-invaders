# Release workflow — DISABLED
#
# Publishing is done manually:
#   1. pnpm build && git add dist/ && git commit
#   2. npm version patch
#   3. git push origin main --tags
#   4. gh release create vX.Y.Z --generate-notes
#   5. git tag -fa v1 -m "v1" && git push origin v1 --force
#
# This workflow previously auto-published on tag push but caused
# duplicate failures when tags were force-updated.

name: Release (disabled)

on:
  workflow_dispatch: # manual-only, not triggered by tag push

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Release workflow is disabled. See file comments for manual release steps."
